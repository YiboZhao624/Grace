model_name_or_path: /root/shared_planing/LLM_model/Qwen3-4B

stage: dpo
pref_beta: 0.1
pref_loss: sigmoid
do_train: true
finetuning_type: full
deepspeed: examples/deepspeed/deepspeed_zero3.json

dataset: trust_align
template: qwen
cutoff_len: 1024
max_samples: 15598
overwrite_cache: true
preprocessing_num_workers: 16

output_dir: /root/projects/RAGRL/baselines/trust-align_reporduce/ckpts
logging_steps: 10
save_steps: 500
plot_loss: true
overwrite_output_dir: true

per_device_train_batch_size: 4
gradient_accumulation_steps: 4 # keep 4 * 2 * 4 = 32 batchsize
learning_rate: 5.0e-7
num_train_epochs: 2
lr_scheduler_type: cosine
warmup_ratio: 0.1
bf16: true
ddp_timeout: 180000000

val_size: 0.1
per_device_eval_batch_size: 4
eval_strategy: steps
eval_steps: 200